[build-system]
requires = ["uv>=0.5.0"]
build-backend = "uv.build"

[project]
name = "kit-tools"
version = "0.1.0"
description = "Codebase search and refactoring tools."
requires-python = ">=3.11"
dependencies = [
  "pytest>=8",
]

[tool.uv]
package = false
